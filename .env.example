# Flask
SECRET_KEY=cambia-esta-clave-por-una-segura
FLASK_DEBUG=1

# PostgreSQL
DATABASE_URL=postgresql+psycopg2://kontratando:kontratando@localhost:5432/kontratando

# OpenAI (usado para auditar CV y respuestas de los candidatos)
OPENAI_API_KEY=sk-tu-llave-de-openai
OPENAI_MODEL=gpt-4o-mini

# Uploads
UPLOAD_FOLDER=uploads
MAX_CONTENT_LENGTH_MB=8

# Admin inicial (se crea automáticamente si no existe ningún admin)
ADMIN_EMAIL=admin@kontratando.com
ADMIN_PASSWORD=CambiaEstaClave123!
